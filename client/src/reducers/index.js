import {
    combineReducers
} from 'redux';

export default combineReducers({});
import {
    combineReducers
} from 'redux';
import alert from './alert';

export default combineReducers({
    alert
});